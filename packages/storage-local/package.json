{
  "name": "@marg/storage-local",
  "version": "1.0.0",
  "private": true,
  "description": "MARG v2 Offline First Local Storage",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "clean": "rimraf dist node_modules"
  },
  "dependencies": {
    "@marg/domain": "workspace:*",
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "rimraf": "^6.0.0"
  }
}
