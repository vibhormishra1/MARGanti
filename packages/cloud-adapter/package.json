{
  "name": "@marg/cloud-adapter",
  "version": "1.0.0",
  "private": true,
  "description": "MARG v2 Tier 2 Cloud Integration — Provider-agnostic cloud adapter layer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rimraf dist node_modules"
  },
  "dependencies": {
    "@marg/domain": "workspace:*",
    "@marg/storage-local": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "rimraf": "^6.0.0"
  }
}
